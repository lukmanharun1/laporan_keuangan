// Sync object
/** @type {import('@jest/types').Config.InitialOptions} */
const config = {
  verbose: true,
  // reporters: [
  //   'default',
  //   'jest-html-reporters'
  // ],
};

module.exports = config;