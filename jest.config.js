// Sync object
/** @type {import('@jest/types').Config.InitialOptions} */
const config = {
  // verbose: true,
  // cache: true
  // reporters: [
  //   'default',
  //   'jest-html-reporters'
  // ],
  testEnvironment: 'node',
  testTimeout: 10000,
};

module.exports = config;